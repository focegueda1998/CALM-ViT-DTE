apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datasets-pvc
spec:
  storageClassName: rook-cephfs-fullerton
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi